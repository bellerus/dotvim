Installation:

    git clone git://github.com/bellerus/dotvim.git ~/.vim

Switch to the `~/.vim` directory, and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update


[optional] Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc

